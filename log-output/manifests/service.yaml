apiVersion: v1
kind: Service
metadata:
  name: log-get-data-svc
spec:
  type: ClusterIP
  selector:
    app: log-get-data
  ports:
    - port: 2346
      protocol: TCP
      targetPort: 3000         # the target port where container(app) listening
