apiVersion: apps/v1
kind: Deployment
metadata:
  name: todo-broadcaster-dep
spec:
  template:
    spec:
      containers:
        - name: todo-broadcaster
          env:
            - name: TBOT_API_TOKEN
              value: "disabled-in-staging"
            - name: PERSONAL_CHAT_ID
              value: "disabled-in-staging"
