apiVersion: apps/v1
kind: Deployment
metadata:
  name: todo-app-dep
spec:
  replicas: 1
  selector:
    matchLabels:
      app: todo-app
  template:
    metadata:
      labels:
        app: todo-app
    spec:
      volumes:
        - name: todo-app-vol
          persistentVolumeClaim:
            claimName: todo-app-claim
      containers:
        - name: todo-app
          image:  ushakiran369/todo-app:1.13
          ports:
            - containerPort: 3000 # Explicitly document the port
          env:
            - name: PORT
              value: "3000"
          volumeMounts:
            - name: todo-app-vol
              mountPath: /usr/src/app
