apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: todo-frontend-pvc
  namespace: project
spec:
  storageClassName: standard-rwo
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
