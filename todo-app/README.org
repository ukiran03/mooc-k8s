*** 1.4. The project, step 2

#+begin_src bash

# Build local Image
docker build -t todo-app.mooc:1.5 .

# Apply manifests
kubectl apply -f manifests

# Checking
kubectl get deployments
kubectl get pods

#+end_src
